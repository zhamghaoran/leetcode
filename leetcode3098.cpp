#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    int sumOfPowers(vector<int> &nums, int k) {
        std::sort(nums.begin(), nums.end());
        long long total = c(nums.size(), k);
    }

    long long c(int x, int y) {
        
    }
};

int main() {

}