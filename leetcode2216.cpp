#include "bits/stdc++.h"

using namespace std;

class Solution {
public:
    int minDeletion(vector<int> &nums) {
        int ans = 0;
        vector<vector<int>> dp(nums.size(), vector<int>(2, 0));
        
    }
};

int main() {
    return 0;
}